# Backend API URL for local development proxy.
# By default, point to the local backend server (running on port 5001).
VITE_API_URL=http://localhost:5001

# To connect to the production backend instead, uncomment the line below:
# VITE_API_URL=https://hungtin-prod.up.railway.app
